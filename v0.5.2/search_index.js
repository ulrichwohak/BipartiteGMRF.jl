var documenterSearchIndex = {"docs":
[{"category":"section","location":"api/#API","page":"API","text":"","title":"API"},{"category":"section","location":"api/#Estimation","page":"API","text":"The estimation entry points extend the Distributions.jl suffstats / fit_mle generic functions, so they compose with using Distributions without name clashes. BipartiteGMRFStats subtypes Distributions.SufficientStats.\n\nData enters as three parallel vectors — firm index, worker index, outcome — with 1-based dense integer indices on each side. The package is an estimator, not a data manipulator: mapping entity identifiers to indices and filtering unusable rows happen before the data reaches it.","title":"Estimation"},{"category":"section","location":"api/#Models","page":"API","text":"","title":"Models"},{"category":"section","location":"api/#Solvers","page":"API","text":"","title":"Solvers"},{"category":"section","location":"api/#StatsAPI-Interface","page":"API","text":"GMRFResult implements StatsAPI.StatisticalModel. coef, coefnames, loglikelihood, nobs, dof, aic, bic, isfitted, and islinear extend StatsAPI; params extends the Distributions.jl generic; converged extends Optim.converged.","title":"StatsAPI Interface"},{"category":"section","location":"api/#Decompositions","page":"API","text":"","title":"Decompositions"},{"category":"section","location":"api/#Covariance","page":"API","text":"","title":"Covariance"},{"category":"section","location":"api/#Simulation","page":"API","text":"","title":"Simulation"},{"category":"section","location":"api/#Non-Backtracking-Diagnostics","page":"API","text":"","title":"Non-Backtracking Diagnostics"},{"category":"method","location":"api/#Distributions.suffstats-Tuple{Type{<:AbstractBipartiteModel}, AbstractVector{<:Integer}, AbstractVector{<:Integer}, AbstractVector{<:Real}}","page":"API","text":"suffstats(::Type{<:AbstractBipartiteModel}, f_idx, w_idx, y;\n          n_firms=maximum(f_idx), n_workers=maximum(w_idx),\n          weighting=Weighting(), model_adjacency=:binary,\n          match_id=nothing, standardize=true)\n\nCompute sufficient statistics for bipartite GMRF MLE from parallel observation vectors: f_idx[k] and w_idx[k] are the 1-based firm and worker indices of observation k, and y[k] its outcome. Repeated (firm, worker) pairs are allowed and handled according to weighting.\n\nReturns a BipartiteGMRFStats that can be passed to fit_mle. Extends Distributions.suffstats.\n\nGraph-only edges\n\nNon-finite values in y (NaN, ±Inf) mark graph-only edges: the corresponding (f_idx[k], w_idx[k]) pair enters the prior adjacency A_prior (and therefore the model's precision matrix and node degrees) but contributes no design row, no term to V'y, y'y, or the observation count K. At least one finite outcome is required.\n\nMatch-grouped observations\n\nWhen match_id is provided, edges sharing a match id form one observation whose design row averages 1/F_s over each distinct firm and 1/M_s over each distinct worker in the match. Outcomes within a match must agree (up to machine precision). K counts matches, not edges. Currently requires Weighting(observations=:raw). Composes with every error model below; the structured ones (error_eta, error_blocks) define their error process on the match units and additionally require each match to span a single firm.\n\nCorrelated errors\n\nerror_cov = R replaces the i.i.d. error covariance σ_ε² I with σ_ε² R: a sparse symmetric matrix over input rows whose connected blocks (read off the sparsity pattern) must each be positive definite — beyond that the blocks are arbitrary, any correlation pattern and any within-block heteroskedasticity. The overall scale of R is not a free parameter: it is rescaled internally to tr(R) = K, so σ_ε² keeps a fixed meaning as the mean error variance regardless of the scale passed in. The typical use is block-diagonal by firm (nonzero entries only between same-firm observations), but the structure is up to the caller. All sufficient statistics become R-weighted (V'R⁻¹V, V'R⁻¹y, y'R⁻¹y) and the constant log det R is carried in the weight statistics, so every downstream solver and the profiled mean structure work unchanged. Requires Weighting(observations=:raw); composes with match_id (R is read at each match's first row, and entries between rows of one match are dropped with the duplicate rows).\n\nGroup-robust errors\n\nerror_groups = g (one group id per input row, typically the firm) makes the fit robust to an arbitrary unknown PD error covariance within each group, in the spirit of clustered standard errors. Observations sharing a group id are collapsed to their group mean (design rows averaged, outcomes averaged), so the unknown within-group error covariance enters the likelihood only through the scalar variance of the group mean. That scalar is σ_ε² ω_c, one free ω per group-size class, estimated jointly with the structural parameters; groups of size error_group_cap (default 8) or larger share the top class. The smallest class present is pinned at ω = 1, which sets the scale of σ_ε (with singleton groups present, σ_ε keeps its usual meaning exactly). Within-group contrasts carry no information under this model — by design: their error covariance is unknown. Fitted ω are reported in the result metadata (error_class_variances, error_class_sizes). Requires Weighting(observations=:raw) and the ExactCholesky solver; composes with match_id (rows of one match must share a group id) and X; mutually exclusive with error_cov.\n\nAR(1) within-firm error correlation\n\nerror_eta = e with e in (-1, 1) (or :estimate to estimate it jointly) models the error covariance as σ_ε² R(eta) with R(eta) block-diagonal by firm and block [eta^|k-l|], where k, l are the caller-supplied edge_index values (a unique permutation of 1..m_i over each firm's observations — its raw rows, or its matches under match_id). edge_index is required and must have the same length as y. Composes with match_id: the AR(1) process is then defined on the match units — a co-managed match is one spell with one error draw, Corr(ε_s, ε_t) = eta^|rank_s − rank_t| over the firm's matches ranked by edge_index (which must be constant within each match; matches tile a firm's timeline disjointly, so ties are structurally impossible and validation is strict). Each match must span a single firm. Requires Weighting(observations=:raw) and the ExactCholesky solver; composes with X (read at each match's first row); mutually exclusive with error_cov and error_groups. Fitted eta is reported as result.eta.\n\nIntegrated per-firm error blocks\n\nerror_blocks = :iw with firm_group (the firm id per row) keeps each firm's observations as one error block whose covariance is integrated out by the EMIWBlocks solver (see its docstring for the model). Composes with match_id: the firm's block is then its matches — error draws live on spells, not member rows — and each match must span a single firm. X is accepted but currently ignored by the EMIW solver (beta comes back nothing); mutually exclusive with the other error models.\n\n\n\n\n\n","title":"Distributions.suffstats"},{"category":"method","location":"api/#Distributions.fit_mle-Tuple{Type{<:AbstractBipartiteModel}, BipartiteGMRFStats}","page":"API","text":"fit_mle(::Type{M}, ss::BipartiteGMRFStats; solver=ExactCholesky(),\n        rho_limit=0.99, fix_rho=nothing, init=nothing, seed=42,\n        strict_forest=false, verbose=false)\n\nFit a bipartite GMRF model by maximum likelihood using precomputed sufficient statistics. Returns a GMRFResult. This extends Distributions.fit_mle and follows its fit_mle(D, suffstats) pattern.\n\nrho_limit=:auto (variance-stable model only) resolves the limit from the non-backtracking spectrum. Use decompose on the result for variance decompositions.\n\nWarm starts (init)\n\ninit is a NamedTuple of starting values, every field optional; whatever is missing keeps the built-in heuristic (rho = min(0.5, 0.5 rho_limit), sigma_a = 0.7, sigma_z = 0.04, sigma_epsilon = 0.4), which assumes a unit-variance outcome and is not tuned per dataset.\n\nr = fit_mle(BipartiteVarianceStableModel, f, w, y)\nr2 = fit_mle(BipartiteVarianceStableModel, f, w, y; init = params(r))\n\nFields: rho, sigma_a, sigma_z, sigma_epsilon, plus rho_eps (effective weighting, default 0.5), eta (error_eta = :estimate, default 0.5), omega (error_groups, default 1 per class), and phi / r / delta (error_blocks = :iw). sigma_epsilon is not an EMIW coordinate: it converts to the error scale through omega_bar = phi*delta/(delta-2), and is ignored with a warning if phi is also given.\n\nA field for a parameter this fit does not estimate is an error. A field for a parameter that is present but held fixed — fix_rho, a numeric error_eta, a numeric rho_eps under effective weighting, or a solver-fixed EMIWBlocks(r=) / (delta=) — is ignored, with a warning unless it already agrees with the pinned value. A nothing-valued field counts as not supplied, so init = params(result) works directly, including when it carries a pinned parameter back into an identically configured fit.\n\nUnits are the original outcome's, matching what the result reports — the scale parameters are divided by the outcome's standard deviation internally, so a warm start taken from a previous GMRFResult or from a raw-data minimum-distance estimate needs no rescaling. omega may be given either as the free classes 2..C or as the full ladder reported in error_class_variances, whose leading entry is pinned at 1.\n\nnote: A warm start is not guaranteed to be faster\nThe Nelder-Mead simplex is built by perturbing each coordinate by 50% (Optim's default), so it still spans a wide region around init. A coordinate sitting at exactly zero on the unconstrained scale instead gets an absolute step of 0.025, and one merely close to zero gets 0.5|x|, which is smaller still; a warm start tends to produce exactly those (log omega = 0, atanh(eta) ~ 0, log sigma ~ 0 at sigma ~ 1), and a simplex that is near-degenerate in one coordinate can satisfy the convergence test almost immediately. ExactCholesky is protected by its L-BFGS polish; HutchSLQ has no polish, so check that a warm-started HutchSLQ fit actually moved. The stopping tolerance is also scaled by the objective at the starting point, which under HutchSLQ means a better start can buy a tighter tolerance and cost extra iterations (ExactCholesky floors it, so it usually does not move). See issue #115.\n\n\n\n\n\n","title":"Distributions.fit_mle"},{"category":"method","location":"api/#Distributions.fit_mle-Tuple{Type{<:AbstractBipartiteModel}, AbstractVector{<:Integer}, AbstractVector{<:Integer}, AbstractVector{<:Real}}","page":"API","text":"fit_mle(::Type{M}, f_idx, w_idx, y; kwargs...)\n\nOne-step convenience: compute sufficient statistics from parallel observation vectors (firm index, worker index, outcome) and fit. Accepts the keyword arguments of both suffstats (n_firms, n_workers, weighting, model_adjacency, match_id, standardize, X, error_cov, error_groups, error_group_cap, error_eta, edge_index, error_blocks, firm_group) and the suffstats-based fit_mle method (including init — see there for the warm-start fields and their units).\n\n\n\n\n\n","title":"Distributions.fit_mle"},{"category":"method","location":"api/#Distributions.fit_mle-Tuple{AbstractBipartiteModel, BipartiteGMRFStats}","page":"API","text":"fit_mle(model::AbstractBipartiteModel, ss::BipartiteGMRFStats;\n        solver=ExactCholesky(), fix_rho=nothing, init=nothing,\n        seed=42, verbose=false)\n\nFit a prebuilt bipartite model (e.g. one constructed with a custom rho_limit or adjacency) by maximum likelihood. Equivalent to solve(model, ss, solver; ...). See the suffstats-based method for the init warm-start fields and their units.\n\n\n\n\n\n","title":"Distributions.fit_mle"},{"category":"method","location":"api/#CommonSolve.solve-Tuple{AbstractBipartiteModel, BipartiteGMRFStats, AbstractGMRFSolver}","page":"API","text":"solve(model::AbstractBipartiteModel, stats::BipartiteGMRFStats, solver::AbstractGMRFSolver;\n      fix_rho=nothing, init=nothing, seed=42, verbose=false)\n\nFit a bipartite GMRF by maximum likelihood with the given solver. Extends CommonSolve.solve, so it composes with using LinearSolve or other SciML-style packages without name clashes. fit_mle is the higher-level entry point and delegates here.\n\nfix_rho fixes the local-dependence parameter during optimization. init warm-starts the optimizer — see fit_mle for its fields and units. Use decompose on the returned result for variance decompositions.\n\n\n\n\n\n","title":"CommonSolve.solve"},{"category":"method","location":"api/#CommonSolve.solve-Tuple{BipartiteVarianceStableModel, BipartiteGMRFStats, EMIWBlocks}","page":"API","text":"solve(model::BipartiteVarianceStableModel, stats::BipartiteGMRFStats, solver::EMIWBlocks;\n      init=nothing, seed=42, verbose=false)\n\nFit the inverse-Wishart error-block model by variational EM, maximizing the ELBO rather than the exact likelihood. fix_rho is not supported here. init warm-starts rho, sigma_a, sigma_z and the error scale (phi, or sigma_epsilon converted through omega_bar = phi*delta/(delta-2)); r and delta are re-estimated by bracketed searches, so passing them seeds only the first E-step. See fit_mle for the full init contract.\n\n\n\n\n\n","title":"CommonSolve.solve"},{"category":"type","location":"api/#BipartiteGMRF.BipartiteGMRFStats","page":"API","text":"BipartiteGMRFStats <: Distributions.SufficientStats\n\nSufficient statistics for bipartite GMRF maximum-likelihood estimation.\n\nContains all data-derived quantities needed by the NLL objective. Computed once via suffstats and reused across all optimizer iterations.\n\n\n\n\n\n","title":"BipartiteGMRF.BipartiteGMRFStats"},{"category":"type","location":"api/#BipartiteGMRF.GMRFResult","page":"API","text":"Fitted bipartite-GMRF model returned by solve and fit_mle.\n\nImplements the StatsAPI.StatisticalModel interface.\n\n\n\n\n\n","title":"BipartiteGMRF.GMRFResult"},{"category":"type","location":"api/#BipartiteGMRF.AbstractBipartiteModel","page":"API","text":"Abstract supertype for bipartite GMRF latent models.\n\nAll bipartite models share a common graph structure (rectangular adjacency matrix, firm/worker degrees) and differ in how the precision matrix is constructed from hyperparameters (ρ, σ_a, σ_z).\n\nSubtypes implement the GaussianMarkovRandomFields.jl LatentModel interface: precision_matrix, mean, hyperparameters, constraints, model_name, length.\n\n\n\n\n\n","title":"BipartiteGMRF.AbstractBipartiteModel"},{"category":"type","location":"api/#BipartiteGMRF.BipartiteNormalizedModel","page":"API","text":"Degree-normalized bipartite GMRF. Implements GaussianMarkovRandomFields.LatentModel.\n\n\n\n\n\n","title":"BipartiteGMRF.BipartiteNormalizedModel"},{"category":"type","location":"api/#BipartiteGMRF.BipartiteUnnormalizedModel","page":"API","text":"Unnormalized D - ρA bipartite GMRF. Implements GaussianMarkovRandomFields.LatentModel.\n\n\n\n\n\n","title":"BipartiteGMRF.BipartiteUnnormalizedModel"},{"category":"type","location":"api/#BipartiteGMRF.BipartiteSpectralModel","page":"API","text":"Spectral-normalized bipartite GMRF. Implements GaussianMarkovRandomFields.LatentModel.\n\n\n\n\n\n","title":"BipartiteGMRF.BipartiteSpectralModel"},{"category":"type","location":"api/#BipartiteGMRF.BipartiteVarianceStableModel","page":"API","text":"Variance-stable bipartite GMRF for forest-like graphs. Implements GaussianMarkovRandomFields.LatentModel.\n\nrho_limit=:auto resolves the limit from the non-backtracking spectrum of the (binarized) adjacency at construction time; the computed NBSpectrum is stored on the model and reused by feasibility.\n\n\n\n\n\n","title":"BipartiteGMRF.BipartiteVarianceStableModel"},{"category":"type","location":"api/#BipartiteGMRF.BipartiteGraph","page":"API","text":"BipartiteGraph\n\nStores the bipartite adjacency matrix and precomputed degree vectors shared by all bipartite model types.\n\n\n\n\n\n","title":"BipartiteGMRF.BipartiteGraph"},{"category":"type","location":"api/#BipartiteGMRF.Weighting","page":"API","text":"Weighting(; observations=:raw, rho_eps=nothing, target=:estimation)\n\nConfigure how repeated firm-worker observations enter the likelihood and variance decompositions.\n\nobservations is :raw (each row an independent draw), :edge (collapse to match means), or :effective (match-effective weights with within-match residual correlation). With :effective, pass rho_eps as a number in [0, 1) to fix the correlation or :estimate to estimate it jointly.\n\n\n\n\n\n","title":"BipartiteGMRF.Weighting"},{"category":"type","location":"api/#BipartiteGMRF.AbstractGMRFSolver","page":"API","text":"Abstract supertype for marginal-likelihood solvers.\n\n\n\n\n\n","title":"BipartiteGMRF.AbstractGMRFSolver"},{"category":"type","location":"api/#BipartiteGMRF.ExactCholesky","page":"API","text":"ExactCholesky(; optim_iters=200, polish=true, autodiff=:finitediff, g_reltol=1e-7,\n              simplex_scale=0.5, simplex_shift=0.025)\n\nDeterministic solver based on sparse Cholesky factorizations.\n\nsimplex_scale and simplex_shift size the initial Nelder-Mead simplex — see HutchSLQ for what they mean and when to lower them.\n\n\n\n\n\n","title":"BipartiteGMRF.ExactCholesky"},{"category":"type","location":"api/#BipartiteGMRF.HutchSLQ","page":"API","text":"HutchSLQ(; logdet_probes=30, lanczos_iters=30, cg_tol=1e-6,\n         cg_maxiter=700, optim_iters=1000, g_reltol=1e-7,\n         simplex_scale=0.5, simplex_shift=0.025)\n\nMatrix-free stochastic solver using Hutchinson stochastic Lanczos quadrature for log determinants and preconditioned conjugate gradients for linear solves.\n\nThe initial simplex\n\nNelder-Mead starts from a simplex built around the starting point x: vertex j+1 differs from x in coordinate j only, and equals (1 + simplex_scale)·x_j + simplex_shift. The defaults are Optim's own.\n\nThe relative term dominates for a coordinate of ordinary magnitude — at the default simplex_scale = 0.5, log σ = −0.9 reaches −1.325, i.e. σ down 35% — so the simplex spans a wide region even when the starting point is already good. Lower it to search locally around a warm start:\n\nExactCholesky(simplex_scale = 0.05)                          # 5% around init\nExactCholesky(simplex_scale = 0.0, simplex_shift = 0.05)     # uniform box\n\nThe absolute term is what saves coordinates near zero. At exactly zero the relative term vanishes and only simplex_shift remains, so setting it to zero makes the simplex degenerate in that coordinate and Nelder-Mead can report convergence having barely moved. A warm start produces exactly those coordinates: log ω = 0, atanh(η) ≈ 0, log σ ≈ 0 at σ ≈ 1, atanh(ρ/rho_limit) ≈ 0 at ρ ≈ 0. ExactCholesky is protected by its L-BFGS polish; HutchSLQ has no polish, so verify that a warm-started HutchSLQ fit actually moved.\n\nSetting both to zero is rejected.\n\nnote: The stopping tolerance also depends on the starting point\ng_reltol is scaled by max(1, |NLL(x₀)|), so a better starting point yields a tighter absolute threshold. ExactCholesky floors it at 1e-3, so it usually does not move there; under HutchSLQ the scaling passes through, and a better start can therefore cost extra iterations rather than fewer. (For a negative NLL the direction reverses.) This is deliberate — see issue #115 — and means \"warm start ⇒ fewer evaluations\" is not guaranteed.\n\n\n\n\n\n","title":"BipartiteGMRF.HutchSLQ"},{"category":"method","location":"api/#StatsAPI.coef-Tuple{GMRFResult}","page":"API","text":"coef(result::GMRFResult)\n\nReturn the fitted parameters as a vector [rho, sigma_a, sigma_z, sigma_epsilon], with rho_eps appended when it was part of the likelihood. Labels are available from coefnames; a named view from params.\n\n\n\n\n\n","title":"StatsAPI.coef"},{"category":"method","location":"api/#StatsAPI.coefnames-Tuple{GMRFResult}","page":"API","text":"coefnames(result::GMRFResult)\n\nReturn the names of the entries of coef.\n\n\n\n\n\n","title":"StatsAPI.coefnames"},{"category":"method","location":"api/#StatsAPI.params-Tuple{GMRFResult}","page":"API","text":"params(result::GMRFResult)\n\nReturn the fitted parameters as a named tuple (rho=..., sigma_a=..., sigma_z=..., sigma_epsilon=..., rho_eps=...), where rho_eps is nothing unless effective weighting was used.\n\n\n\n\n\n","title":"StatsAPI.params"},{"category":"method","location":"api/#StatsAPI.loglikelihood-Tuple{GMRFResult}","page":"API","text":"loglikelihood(result::GMRFResult)\n\nReturn the maximized log-likelihood of the (mean-centered) data in original outcome units, including the Gaussian normalizing constant and the Jacobian of the internal standardization. Unlike nll — the raw optimizer objective — this value is invariant to the standardize option and comparable across fits of the same data.\n\n\n\n\n\n","title":"StatsAPI.loglikelihood"},{"category":"method","location":"api/#StatsAPI.nobs-Tuple{GMRFResult}","page":"API","text":"nobs(result::GMRFResult)\n\nReturn the number of model observations used by the likelihood.\n\n\n\n\n\n","title":"StatsAPI.nobs"},{"category":"method","location":"api/#StatsAPI.dof-Tuple{GMRFResult}","page":"API","text":"dof(result::GMRFResult)\n\nReturn the number of estimated parameters. The baseline (ρ, σa, σz, σε) counts four; a `fixrhofit counts one fewer, and a jointly estimatedrho_eps=:estimate` counts one more. Parameters held fixed do not count.\n\n\n\n\n\n","title":"StatsAPI.dof"},{"category":"method","location":"api/#StatsAPI.aic-Tuple{GMRFResult}","page":"API","text":"aic(result::GMRFResult)\n\nAkaike Information Criterion: -2logℓ + 2k.\n\n\n\n\n\n","title":"StatsAPI.aic"},{"category":"method","location":"api/#StatsAPI.bic-Tuple{GMRFResult}","page":"API","text":"bic(result::GMRFResult)\n\nBayesian Information Criterion: -2logℓ + k⋅log(n).\n\n\n\n\n\n","title":"StatsAPI.bic"},{"category":"method","location":"api/#StatsAPI.isfitted-Tuple{GMRFResult}","page":"API","text":"isfitted(result::GMRFResult)\n\nReturn true; a GMRFResult always represents a completed fit.\n\n\n\n\n\n","title":"StatsAPI.isfitted"},{"category":"method","location":"api/#StatsAPI.islinear-Tuple{GMRFResult}","page":"API","text":"islinear(result::GMRFResult)\n\nReturn false; the bipartite GMRF likelihood is not linear in its parameters.\n\n\n\n\n\n","title":"StatsAPI.islinear"},{"category":"function","location":"api/#BipartiteGMRF.nll","page":"API","text":"nll(result::GMRFResult)\n\nReturn the fitted value of the internal optimizer objective: the negative log-likelihood of the standardized data with additive constants dropped. Use loglikelihood for the constant-complete, original-units value.\n\n\n\n\n\n","title":"BipartiteGMRF.nll"},{"category":"method","location":"api/#Optim.converged-Tuple{GMRFResult}","page":"API","text":"converged(result::GMRFResult)\n\nReturn whether the optimizer reported convergence. Extends Optim.converged.\n\n\n\n\n\n","title":"Optim.converged"},{"category":"function","location":"api/#BipartiteGMRF.decompose","page":"API","text":"decompose(result::GMRFResult; kind=:model, probes=200, seed=42,\n          target=result.stats.weighting.target, verbose=false)\n\nEstimate the variance decomposition implied by a fitted bipartite GMRF.\n\nReturns a VarianceDecomposition containing firm, worker, cross, residual, and total variance components.\n\nkind selects the decomposition type:\n\n:model — decomposition from the GMRF's precision structure alone\n:fitted — includes fitted effects (posterior mode + trace correction)\n\n\n\n\n\n","title":"BipartiteGMRF.decompose"},{"category":"type","location":"api/#BipartiteGMRF.VarianceDecomposition","page":"API","text":"Variance decomposition returned by decompose(result; kind=:model) or decompose(result; kind=:fitted).\n\n\n\n\n\n","title":"BipartiteGMRF.VarianceDecomposition"},{"category":"function","location":"api/#BipartiteGMRF.covariance","page":"API","text":"covariance(result::GMRFResult; kind=:model, units=:original)\n\nFactor the fitted precision matrix for covariance extraction.\n\nkind selects the precision matrix:\n\n:model — the GMRF's precision Q\n:fitted — the data-augmented precision Q + λV'V\n\nunits may be :original or :scaled. Pass the returned CovarianceOperator to cov_block to extract selected entity blocks.\n\n\n\n\n\n","title":"BipartiteGMRF.covariance"},{"category":"function","location":"api/#BipartiteGMRF.cov_block","page":"API","text":"cov_block(op::CovarianceOperator; firms=[], workers=[],\n          row_firms=[], row_workers=[], col_firms=[], col_workers=[],\n          batch_size=16)\n\nExtract a principal or rectangular covariance block from a cached covariance operator. Firms and workers are addressed by their 1-based node indices (the same indices passed to suffstats).\n\nUse firms/workers for a principal block, or row_* and col_* keywords for a rectangular block. The result is a CovarianceBlock with matrix values and row/column entity metadata.\n\nbatch_size controls how many right-hand-side unit columns are solved at once. Larger batches can reduce factorization solve overhead, while smaller batches reduce temporary memory. The extraction allocates the returned block plus an n_latents * batch_size dense right-hand-side workspace.\n\n\n\n\n\n","title":"BipartiteGMRF.cov_block"},{"category":"type","location":"api/#BipartiteGMRF.CovarianceOperator","page":"API","text":"Cached covariance factorization returned by covariance(result; kind=:model) or covariance(result; kind=:fitted).\n\n\n\n\n\n","title":"BipartiteGMRF.CovarianceOperator"},{"category":"type","location":"api/#BipartiteGMRF.CovarianceBlock","page":"API","text":"Covariance block returned by cov_block.\n\n\n\n\n\n","title":"BipartiteGMRF.CovarianceBlock"},{"category":"function","location":"api/#BipartiteGMRF.simulate","page":"API","text":"simulate(model::AbstractBipartiteModel, firm_ids, worker_ids;\n         ρ, σ_a, σ_z, σ_ε, rng=Random.default_rng())\n\nSimulate outcomes from a bipartite GMRF.\n\nDraws latent effects θ = [α; z] ~ GMRF(0, Q(ρ, σ_a, σ_z)) and generates observations y_k = α[firm_ids[k]] + z[worker_ids[k]] + ε_k where ε_k ~ N(0, σ_ε²).\n\nReturns a NamedTuple with fields:\n\ny: simulated outcome vector\nfirm_effects: sampled firm effects α\nworker_effects: sampled worker effects z\nfirm_ids, worker_ids: the input ID vectors\n\nResiduals are drawn i.i.d.; the within-match residual correlation rho_eps of Weighting(observations=:effective) is not simulated. To simulate that data-generating process, add a match-level N(0, √rho_eps ⋅ σ_ε) draw shared by repeated (firm, worker) pairs on top of an i.i.d. part scaled by √(1 − rho_eps).\n\n\n\n\n\nsimulate(model::AbstractBipartiteModel, A::SparseMatrixCSC;\n         ρ, σ_a, σ_z, σ_ε, rng=Random.default_rng())\n\nSimulate outcomes from a bipartite GMRF using an adjacency matrix to define firm-worker pairs. Each nonzero entry A[i,j] generates one observation.\n\n\n\n\n\n","title":"BipartiteGMRF.simulate"},{"category":"type","location":"api/#BipartiteGMRF.NBSpectrum","page":"API","text":"Non-backtracking spectrum diagnostics for a bipartite prior graph.\n\nlambda_nb is the largest radius among connected 2-core components. Node-level vectors use the latent ordering (firms followed by workers). node_scores are normalized within each 2-core component, core_component is zero outside the 2-core, and distance_to_core is -1 in components without a cycle. eigenvalues contains at most the requested number of largest-modulus values from each component.\n\nIf any iterative eigensolve fails, converged is false and lambda_nb is NaN; the corresponding component also carries NaN scores.\n\n\n\n\n\n","title":"BipartiteGMRF.NBSpectrum"},{"category":"function","location":"api/#BipartiteGMRF.nb_spectrum","page":"API","text":"nb_spectrum(A_prior; seed=12345, nev=6, tol=1e-7)\n\nCompute non-backtracking spectral diagnostics from a rectangular sparse prior adjacency matrix. Each connected 2-core component is solved separately using an Ihara-Bass companion matrix. Forests return lambda_nb == 0; simple cycle components return radius one.\n\nLarge components use ArnoldiMethod with a local, explicitly seeded start vector. The input matrix is treated as binary support and is not modified.\n\n\n\n\n\n","title":"BipartiteGMRF.nb_spectrum"},{"category":"function","location":"api/#BipartiteGMRF.feasibility","page":"API","text":"feasibility(model::BipartiteVarianceStableModel; seed=12345, kwargs...)\n\nReport variance-stable feasibility for a model. The result contains the non-backtracking spectrum and radius, the model-theoretic rho_ceiling, the active numeric rho_limit, the recommended guarded limit, its source, and whether the active limit is below the ceiling.\n\nA model constructed with rho_limit=:auto reuses its construction-time spectrum. Explicit numeric limits are audited without being changed; an unsafe explicit limit emits a warning and remains active for estimation.\n\n\n\n\n\n","title":"BipartiteGMRF.feasibility"},{"category":"function","location":"api/#BipartiteGMRF.rho_at_bound","page":"API","text":"rho_at_bound(result)\n\nReturn true when a freely estimated variance-stable rho uses at least 98% of its active optimization limit. Fixed-rho and non-variance-stable fits return false.\n\n\n\n\n\n","title":"BipartiteGMRF.rho_at_bound"},{"category":"section","location":"performance/#Performance","page":"Performance","text":"The package does not set global BLAS thread counts at load time. For many sparse Cholesky and iterative-solver workloads, using one BLAS thread can improve repeatability and avoid oversubscription:\n\nusing LinearAlgebra\nBLAS.set_num_threads(1)\n\nMake that choice in applications or scripts, not in package code.\n\nFor larger graphs, prefer HutchSLQ() and tune logdet_probes, lanczos_iters, cg_tol, and cg_maxiter against the desired runtime and stochastic tolerance.","title":"Performance"},{"category":"section","location":"performance/#Warm-starts","page":"Performance","text":"The default starting point is a fixed heuristic — sigma_a = 0.7, sigma_z = 0.04, sigma_epsilon = 0.4 on a unit-variance outcome — and is not adapted to the data. init replaces it, field by field, in the outcome's own units:\n\nr  = fit_mle(BipartiteVarianceStableModel, f, w, y)\nr2 = fit_mle(BipartiteVarianceStableModel, f, w, y;\n             error_groups = firm, init = params(r))\n\nTwo uses. The first is cost: several error models fitted on the same graph can share one cheap pilot estimate instead of each climbing from the same dataset-agnostic default. The second is diagnostic: when a fit lands on a boundary — rho pinned at rho_limit, a variance collapsed to zero — refitting from a different, informed starting point is what separates a genuine feature of the likelihood from an artifact of where the optimizer began.\n\nA warm start on its own is not guaranteed to be faster, and the reasons are worth knowing before reading too much into a timing.","title":"Warm starts"},{"category":"section","location":"performance/#Size-the-simplex-to-match-the-start","page":"Performance","text":"Nelder-Mead does not begin at a point but at a simplex around it: vertex j+1 differs from the start in coordinate j only, and equals (1 + simplex_scale)·x_j + simplex_shift. At the default simplex_scale = 0.5 that is a 50% relative perturbation — enormous on the unconstrained scale, where log σ = −0.9 reaches −1.325, i.e. σ down 35%. So a warm-started fit with default settings still searches a wide region, and most of the benefit of init is thrown away. Shrink the simplex to keep the search local:\n\n# search ±5% around a good starting point\nfit_mle(BipartiteVarianceStableModel, f, w, y;\n        solver = ExactCholesky(simplex_scale = 0.05),\n        init   = params(pilot))\n\n# or a uniform absolute box, independent of coordinate magnitude\nExactCholesky(simplex_scale = 0.0, simplex_shift = 0.05)\n\nsimplex_shift is the absolute term, and it is what rescues coordinates near zero: at exactly zero the relative term vanishes and only the shift remains. A warm start produces exactly those coordinates — log omega = 0, atanh(eta) ≈ 0, log sigma ≈ 0 at sigma ≈ 1, atanh(rho/rho_limit) ≈ 0 at rho ≈ 0 — so a simplex that is degenerate in one of them can satisfy the convergence test having barely moved. Setting both knobs to zero is rejected for that reason. ExactCholesky recovers through its L-BFGS polish; HutchSLQ has no polish, so check that a warm-started HutchSLQ fit actually moved.","title":"Size the simplex to match the start"},{"category":"section","location":"performance/#The-stopping-tolerance-also-depends-on-the-start","page":"Performance","text":"g_reltol is scaled by max(1, |NLL(x₀)|), so a better starting point yields a tighter absolute threshold. ExactCholesky floors it at 1e-3, so it usually does not move there; under HutchSLQ the scaling passes through and a better start can cost extra iterations rather than fewer. (For a negative NLL the direction reverses.) This is deliberate — pinning it would silently change when existing fits stop.","title":"The stopping tolerance also depends on the start"},{"category":"section","location":"models/#Models","page":"Models","text":"Model types describe the precision structure of the latent firm and worker effects. They are intentionally separate from solvers. All model types subtype AbstractBipartiteModel <: GaussianMarkovRandomFields.LatentModel and are constructed from a rectangular sparse adjacency matrix (firms × workers):\n\nBipartiteNormalizedModel(A; rho_limit=0.99): default degree-normalized precision model.\nBipartiteUnnormalizedModel(A; rho_limit=0.99): paper-style D - rho*A precision model.\nBipartiteSpectralModel(A; seed=12345, rho_limit=0.99): spectral-normalized adjacency model. The seed controls the power-iteration initialization used for spectral normalization.\nBipartiteVarianceStableModel(A; strict_forest=false, rho_limit=0.99): variance-stable precision model for forest-like graphs. Cyclic graphs warn by default; with strict_forest=true they throw ArgumentError. Pass rho_limit=:auto to opt into a non-backtracking feasibility limit resolved at construction; the computed spectrum is stored on the model.\n\nWhen fitting with fit_mle(ModelType, f, w, y) or fit_mle(ModelType, ss) the model is built internally from the sufficient statistics' prior adjacency; construct a model directly (and pass it to fit_mle(model, ss)) when you need a custom adjacency or limit.\n\nrho_limit sets the open interval (-rho_limit, rho_limit) used to transform and validate the latent firm-worker dependence parameter. The default rho_limit=0.99 preserves the historical numerical optimization bound; it is not a model-theoretic restriction.\n\nFor an automatic variance-stable limit, forests resolve to 0.99; cyclic graphs resolve to min(0.99, 0.98 / lambda_nb). Construction always emits an information message and stores the spectrum, its source, and the active limit on the model (model.spectrum, model.rho_limit_source). Numeric limits remain explicit expert overrides. Call feasibility(model) to audit one; an unsafe limit warns but is not changed. Every variance-stable fit reports its limit utilization, and rho_at_bound(result) identifies a freely estimated rho at 98% or more of the active limit.","title":"Models"},{"category":"section","location":"models/#Direct-GMRF-Access","page":"Models","text":"The model types implement the GaussianMarkovRandomFields.jl LatentModel interface, so a parameterized model is one call away from a full GMRF:\n\ngmrf = model(; ρ=0.5, σ_a=1.0, σ_z=0.8)\nx = rand(gmrf)\nv = GaussianMarkovRandomFields.var(gmrf)","title":"Direct GMRF Access"},{"category":"section","location":"models/#Supported-Solver-Matrix","page":"Models","text":"BipartiteNormalizedModel, BipartiteUnnormalizedModel, and BipartiteVarianceStableModel support ExactCholesky() and HutchSLQ(). BipartiteSpectralModel supports HutchSLQ() only. On cyclic variance-stable graphs, direct factorization can incur substantial fill-in; choose the solver to match graph size and sparsity. Unsupported combinations throw ArgumentError before fitting.","title":"Supported Solver Matrix"},{"category":"section","location":"examples/#Examples","page":"Examples","text":"Throughout, f, w, and y are parallel vectors: f[k] and w[k] are the 1-based firm and worker indices of observation k, and y[k] its outcome. If your data lives in a table with entity identifiers, map the identifiers to dense indices first, e.g.\n\nfirm_index = Dict(id => i for (i, id) in enumerate(unique(table.firm_id)))\nf = [firm_index[id] for id in table.firm_id]","title":"Examples"},{"category":"section","location":"examples/#One-Step-API","page":"Examples","text":"using BipartiteGMRF\n\nresult = fit_mle(BipartiteNormalizedModel, f, w, y;\n    solver=ExactCholesky(),\n    seed=42,\n)","title":"One-Step API"},{"category":"section","location":"examples/#Two-Step-API","page":"Examples","text":"Precompute sufficient statistics once and reuse them across fits:\n\nss = suffstats(BipartiteNormalizedModel, f, w, y;\n    weighting=Weighting(observations=:raw),\n)\n\nresult = fit_mle(BipartiteNormalizedModel, ss;\n    solver=ExactCholesky(),\n    seed=42,\n)\n\nTo fit a custom-built model (for example with a hand-picked rho_limit):\n\nmodel = BipartiteNormalizedModel(ss.A_prior; rho_limit=0.9)\nresult = fit_mle(model, ss; solver=ExactCholesky())","title":"Two-Step API"},{"category":"section","location":"examples/#StatsAPI-Accessors","page":"Examples","text":"using StatsAPI\n\ncoef(result)          # [rho, sigma_a, sigma_z, sigma_epsilon]\ncoefnames(result)     # [\"rho\", \"sigma_a\", \"sigma_z\", \"sigma_epsilon\"]\nparams(result)        # (rho=..., sigma_a=..., sigma_z=..., sigma_epsilon=..., rho_eps=...)\nloglikelihood(result) # original-units log-likelihood, constants included\naic(result); bic(result); dof(result); nobs(result)","title":"StatsAPI Accessors"},{"category":"section","location":"examples/#Variance-Decomposition","page":"Examples","text":"model_vd  = decompose(result; kind=:model,  probes=100, seed=42)\nfitted_vd = decompose(result; kind=:fitted, probes=100, seed=42)","title":"Variance Decomposition"},{"category":"section","location":"examples/#Effective-Weighting","page":"Examples","text":"result = fit_mle(BipartiteNormalizedModel, f, w, y;\n    weighting=Weighting(observations=:effective, rho_eps=:estimate),\n    solver=ExactCholesky(),\n)\nresult.rho_eps        # estimated within-match residual correlation","title":"Effective Weighting"},{"category":"section","location":"examples/#Simulation","page":"Examples","text":"using SparseArrays, Random\n\nA = sparse([1, 1, 2, 2, 3], [1, 2, 2, 3, 3], ones(5), 3, 3)\nmodel = BipartiteNormalizedModel(A)\nsim = simulate(model, A; ρ=0.5, σ_a=1.0, σ_z=0.8, σ_ε=0.3,\n               rng=MersenneTwister(42))","title":"Simulation"},{"category":"section","location":"solvers/#Solvers","page":"Solvers","text":"ExactCholesky(; optim_iters=200, polish=true, autodiff=:finitediff, g_reltol=1e-7)\nHutchSLQ(; logdet_probes=30, lanczos_iters=30, cg_tol=1e-6, cg_maxiter=700, optim_iters=1000, g_reltol=1e-7)\n\nExactCholesky() uses sparse Cholesky factorizations for deterministic likelihood evaluation: a gradient-free Nelder-Mead search, followed (with polish=true) by a finite-difference L-BFGS polish. HutchSLQ() uses PCG and stochastic Lanczos quadrature for larger graphs.\n\nFor BipartiteVarianceStableModel, HutchSLQ() evaluates the log-determinant ratio as logdet(B + lambda*S*VtV*S) - logdet(B), using identical random probes for both terms. This avoids cancellation between sigma-dependent log determinants when one latent standard deviation is small.\n\nBoth solvers accept seed through fit_mle or solve to make stochastic paths reproducible.\n\nHutchSLQ's g_reltol sets the Nelder-Mead convergence tolerance relative to the objective magnitude at the start point: the optimizer stops once the simplex-objective spread falls below g_reltol * max(1, |nll₀|). A relative tolerance is scale-invariant, so the same setting converges on both small and very large graphs. The stochastic SLQ/PCG objective is accurate only to a relative level, so an absolute tolerance tight enough for a small graph is unreachable on a large one and would force the optimizer to exhaust optim_iters without converging. ExactCholesky uses the same g_reltol but with a 1e-3 absolute floor — g_tol = max(1e-3, g_reltol * max(1, |nll₀|)) — so its small/medium-graph behaviour is unchanged while large exact problems still converge promptly.","title":"Solvers"},{"category":"section","location":"weighting/#Weighting","page":"Weighting","text":"Weighting(; observations=:raw, rho_eps=nothing, target=:estimation) configures observation weighting and the default decomposition target.\n\nObservation weighting controls how repeated firm-worker observations enter the measurement model:\n\n:raw uses every observation row.\n:edge collapses repeated firm-worker pairs to edge means.\n:effective applies compound-symmetric residual effective weights using a fixed rho_eps or rho_eps=:estimate.\n\nVariance decompositions can target :estimation, :personyear, or :edge.","title":"Weighting"},{"category":"section","location":"covariance/#Covariance","page":"Covariance","text":"Covariance extraction factors the fitted model or data-augmented precision matrix and extracts requested entity blocks by batched solves.\n\ncovariance(result; kind=:model, units=:original): factor the fitted model precision Q.\ncovariance(result; kind=:fitted, units=:original): factor the data-augmented precision Q + λV'V.\ncov_block(op; ..., batch_size=16): extract principal or rectangular blocks by firm and worker node indices (the same 1-based indices passed to suffstats).\nCovarianceOperator: cached factorization and metadata for extraction.\n\nunits=:original reports covariances in original outcome units; units=:scaled reports them in internal standardized units.","title":"Covariance"},{"category":"section","location":"covariance/#Batch-Size-And-Memory","page":"Covariance","text":"cov_block extracts selected columns of the inverse precision matrix by solving against batches of unit vectors. batch_size controls how many requested columns are solved at once. Larger batches can reduce sparse factor solve overhead, but allocate a dense temporary right-hand-side workspace with n_latents * batch_size entries, in addition to the returned covariance block. Lower batch_size when extracting from large fitted graphs under tight memory limits.\n\nExamples:\n\nmodel_op = covariance(result; kind=:model, units=:original)\nfitted_op = covariance(result; kind=:fitted, units=:scaled)\n\nprincipal = cov_block(model_op; firms=[1, 2], workers=[1, 2])\nrect = cov_block(fitted_op; row_firms=[1], col_workers=[1, 2])","title":"Batch Size And Memory"},{"category":"section","location":"#BipartiteGMRF.jl","page":"Overview","text":"BipartiteGMRF.jl fits Gaussian Markov random-field random-effects models on bipartite graphs. The library API is arrays-in and typed-struct-out: callers provide the graph as parallel firm-index / worker-index vectors together with an outcome vector, choose a model type, solver, and weighting, and receive a GMRFResult.\n\nThe core model is\n\ny_e = a_f(e) + z_w(e) + epsilon_e\n\nwith jointly Gaussian firm and worker effects. The package supports marginal likelihood fitting, model and fitted variance decomposition, covariance block extraction from fitted models, and simulation from the latent field.\n\nEstimation follows the Distributions.jl suffstats / fit_mle convention — the package extends those generic functions — and fitted results implement StatsAPI.StatisticalModel.","title":"BipartiteGMRF.jl"},{"category":"section","location":"#Package-Boundary","page":"Overview","text":"The package is an estimator, not a data manipulator. It does not read or write files, does not know about tables or column names, and does not drop or repair observations. Application code maps entity identifiers to dense 1-based integer indices, filters unusable rows, and passes plain vectors.","title":"Package Boundary"},{"category":"section","location":"#Quick-Example","page":"Overview","text":"using BipartiteGMRF\n\nf = [1, 1, 2, 2]        # firm index per observation\nw = [1, 2, 2, 3]        # worker index per observation\ny = [1.0, 0.8, 1.2, 0.7]\n\nresult = fit_mle(BipartiteNormalizedModel, f, w, y;\n                 solver=ExactCholesky(), seed=42)\n\nresult.rho                    # local dependence parameter\ncoef(result)                  # [rho, sigma_a, sigma_z, sigma_epsilon]\nloglikelihood(result)         # StatsAPI log-likelihood\n\nfitted_vd = decompose(result; kind=:fitted, probes=50, seed=42)\nblock = cov_block(covariance(result; kind=:model); firms=[1], workers=[1])\n\nSee the Examples page for the two-step suffstats / fit_mle workflow and weighting options.","title":"Quick Example"},{"category":"section","location":"nonbacktracking/#Non-Backtracking-Diagnostics","page":"Non-Backtracking","text":"nb_spectrum(A_prior) computes the non-backtracking radius and localization diagnostics of the binary prior graph from a rectangular sparse adjacency matrix (for a computed sufficient-statistics object, pass ss.A_prior).\n\nspectrum = nb_spectrum(ss.A_prior; seed=12345)\nspectrum.lambda_nb\nspectrum.in_two_core\nspectrum.node_scores\n\nThe implementation peels the graph to its 2-core and solves each connected core component separately through the sparse Ihara-Bass companion matrix. It does not construct the directed-edge Hashimoto matrix and does not depend on a graph package. Forests have lambda_nb == 0; a simple cycle has lambda_nb == 1.\n\nNode-level vectors follow the package's latent ordering: firms first, then workers. Localization scores are nonnegative and normalized within each core component. Nodes in components without a cycle have distance_to_core == -1.\n\nLarge core components use Arnoldi iteration with a local seeded start vector. Always check spectrum.converged before using the radius: a failed component sets the global lambda_nb to NaN rather than returning a partial bound.\n\nFor BipartiteVarianceStableModel(A; rho_limit=:auto) (or fit_mle(BipartiteVarianceStableModel, f, w, y; rho_limit=:auto)), model construction uses this diagnostic to resolve a guarded optimization limit and stores the spectrum on the model. The default numeric rho_limit=0.99 remains the explicit behavior. feasibility(model) audits an explicit limit against the ceiling after the fact.","title":"Non-Backtracking Diagnostics"}]
}
